#include "main.h"
/**
 * main - Entry point
 * Description: A program that prints the alphabets in lower case;
 * Return: Always 0.
 */
void print_alphabet(void)
{
	int i;

	for (i = 'a'; i <= 'z'; i++)

	{
		_putchar(i);
	}
	_putchar('\n');
}
